name = "ecosync-api"
compatibility_date = "2024-01-01"

# Define routes for different functions
[[routes]]
pattern = "*/scan-fridge"
script = "scan-fridge"

[[routes]]
pattern = "*/generate-recipes"
script = "generate-recipes"

# GEMINI_API_KEY should be set via: wrangler secret put GEMINI_API_KEY