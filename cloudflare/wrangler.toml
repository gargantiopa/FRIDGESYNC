name = "ecosync-api"
main = "scan-fridge.js"
compatibility_date = "2024-01-01"

# GEMINI_API_KEY should be set via: wrangler secret put GEMINI_API_KEY
# For generate-recipes worker, create separate config or use routes